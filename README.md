# This is for Ubuntu systems right now because it's all I use lol

this isn't anything fancy, just installs and sets zsh as default, makes sure you have git and curl, installs oh my zsh and makes a backup, then it'll prompt you for any themes you may want to apply, and then it will prompt you to provide any default omz plugins to apply or you can supply a git repo for it to append.
